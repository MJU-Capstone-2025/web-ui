import "./Header.css";

const Header: React.FC = () => {
    return (
        <header id="main-header">
            <h4 className="logo" id="header-logo">
                가격 예측 모델
            </h4>
        </header>
    );
};

export default Header;
